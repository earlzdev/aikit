---
description: Set aikit up in this project — detect zones and verify commands, write aikit.yml, prove the pieces run
---

Invoke the `init` skill.

Anything the owner said here, treat as answers already given — do not ask again:

$ARGUMENTS
