---
description: Put a finished milestone through acceptance — a separate agent walks it by hand on a disposable stand, without the diff
argument-hint: <milestone>
---

Invoke the `acceptance-loop` skill for this milestone:

$ARGUMENTS

Read the milestone from the plan file and pass its full text, its "done when"
checklist and the owner's scenario as the brief. If the milestone's acceptance
line is missing, stop and ask — a missing line is not a "no".
